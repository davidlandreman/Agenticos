# Committed compiler-compat ELFs

`CCCRT.ELF`, `CCLIBC.ELF`, and `CCPROBE.ELF` are generated from
`userland/apps/compiler-compat/` and are mandatory inputs to the booted kernel
test suite. See that directory's README for the coverage ladder and refresh
command.
