[package]
name = "runtime"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "runtime"
path = "src/lib.rs"

[features]
default = ["standalone"]
standalone = ["dep:linked_list_allocator"]
embedded = []

[dependencies]
linked_list_allocator = { version = "0.10", optional = true }
